These components are automatically added from https://shadcn-svelte.com using the command line. 

You could customize them, but be careful not to overwrite them when you run the `shadcn add` command again.